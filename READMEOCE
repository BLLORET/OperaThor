À changer :

Laisse le nom de variables de a, b, c, d. Ça t'évites de tout changer.
	Modifier : On veut juste l'équation maintenant
		if argc < 2
	
	Modifier : mettre tes nb de tableau
        	//Initialisation et attribution
		int degre = atoi(argv[5]);
		int a= atoi(argv[2]);
		int b= atoi(argv[3]);
		int c= atoi(argv[4]);
	
      	Supprimer: Pas besoin je penses
		char chiffres[14]={};
		int numbers[12]={};

	Modifier : ça sert juste à avoir le int après le = donc du coup chez toi 			   c'est dans ton tableau de droite je crois

		gauche(argv[1],chiffres);
	        recup_chiffres(chiffres,numbers);
		int d = droit(argv[1]);
